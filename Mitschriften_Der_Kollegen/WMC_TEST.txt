Migration erstellen -> API als StartupProject, Infrastructure als Default Project
-> "Add-Migration Initial"
-> "Update-Database"

Features anlegen -> Command, Query






DataSeeder wird vorgegeben sein -> entitäten auch

Zu tun:
Validation(Application, Domain)
Controller
Queries, Commands

Bei Update: int id und command.id prüfen ob diese gleich sind -> wenn nein im controller error.


In PP-> 
GRÜN: Ändert sich nie
ROT: ändert sich (muss ich machen!)


Wenn DI gebraucht wird -> in DependencyInjection eintragen (z.B.: UniquenessChecker)
Bei spezielle Abfragen z.B.: Erste100MessungenMitXBeginennd -> im Repository als Methode!



ALS ERSTES ENTITIES ANLEGEN (OHNE VALIDATION AM ANFANG!)
ADD / DELETE FÜR DIESE ENTITITES
